# Migration Agent Permissions

Default permissions:

- read

Future permissions must be explicitly approved before writing migrated records, sending invitations, changing memberships, or modifying production data.

